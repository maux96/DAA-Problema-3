# import brute_force
# import heuristica
import test_cases
if __name__=='__main__':
    while (True):
        n = int(input())
        test_cases.start(n)






    # # k: cantiad de cursos
    # # n: total de propuestas
    # # lists: lista de propuestas
    # k, n=map(int,input().split())
    # lists=[]

    # for i in range(n):
    #     lists.append(set(map(int, input().split())))
    # print(brute_force.solution(k, lists))
    # print(heuristica.first_min_intersection(k, lists))

